version: 0.2
    
phases:
  install:
    runtime-versions:
      docker: 20
    commands:
      - make install
  testing & linting:
    commands:
      - make lint
      - make test
  post_build:
    commands:
      - echo Testing completed on `data`